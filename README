Content of git respository
==========================

-   framework:
        Basic framework which provides the possibility to manage, launch
        and use ROS nodes remotely.

-   test:
        Different packages which can be used with the framework.
